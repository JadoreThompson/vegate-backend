name: Test

on:
  push:
    branches: ["development"]

jobs:
  push_changes:
    runs-on: ubuntu-slim
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}

      - name: Something
        run: echo "Hello world"