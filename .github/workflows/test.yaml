name: Test

on:
  push:
    branches: ["development"]

jobs:
  push_changes:
    runs-on: ubuntu-slim
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}

      - name: Run tests
        run: uv run pytest -s -v